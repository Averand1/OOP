name = "Zack"
print(len(name))

some_list = ["some", "name"]
print(len(some_list))

#You can see how len function has diff form which handles list elements
#That's polymorphism in action, a single funcion does now
# how to handle different kinds of objects as expected!